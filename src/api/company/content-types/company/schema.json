{
  "kind": "collectionType",
  "collectionName": "companies",
  "info": {
    "singularName": "company",
    "pluralName": "companies",
    "displayName": "Firma",
    "description": ""
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "company_name": {
      "type": "string",
      "required": true
    },
    "status": {
      "type": "enumeration",
      "enum": [
        "aktiv",
        "gelöscht",
        "Liquidation",
        "Gesellschaft verlassen"
      ],
      "default": "aktiv"
    },
    "hr_dept": {
      "type": "enumeration",
      "enum": [
        "HRA",
        "HRB"
      ],
      "required": true
    },
    "hr_number": {
      "type": "integer",
      "required": true,
      "unique": true
    },
    "hr_court": {
      "type": "string"
    },
    "lei": {
      "type": "string"
    },
    "main_branch": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "api::place.place",
      "inversedBy": "companies"
    },
    "branches": {
      "type": "relation",
      "relation": "manyToMany",
      "target": "api::place.place",
      "inversedBy": "companies_branches"
    }
  }
}
